[defaults]
inventory = inventories/prod/hosts.yml
roles_path = roles
# Single identified target — ensure the Pi's host key is in ~/.ssh/known_hosts before running.
host_key_checking = True
retry_files_enabled = False
stdout_callback = yaml
interpreter_python = auto_silent

[ssh_connection]
pipelining = True
