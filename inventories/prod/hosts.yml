---
all:
  children:
    pi5:
      hosts:
        # Single identified target: Raspberry Pi 5 + AI HAT+ 2.
        # Update ansible_host to match your device's IP address before first run.
        pi5-node:
          ansible_host: 192.168.1.50
          ansible_user: "{{ pi_user }}"
